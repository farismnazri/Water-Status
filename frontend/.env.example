# Preferred API base variable (used first)
VITE_API_BASE_URL=https://api.water-status.shop

# Legacy alias (optional; only used when VITE_API_BASE_URL is not set)
# VITE_API_BASE=https://api.water-status.shop
